cp -v configure{,.orig}
sed 's:/usr/local/bin:/bin:' configure.orig > configure

# configure and build

./configure --prefix=/tools       \
            --with-tcl=/tools/lib \
            --with-tclinclude=/tools/include

make
